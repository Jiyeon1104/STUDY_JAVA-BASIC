선언문 <%! %> 자바 변수(전역 변수)나 매서드 등을 정의함
표현문 <%= %> 변수, 계산식 매서드 등의 결과를 문자열로 출력
스크립틀릿 <% %> 자바 코드를 작성하는데 사용됨 (가장 많이 사용)

스크립트 태그는 크게 세 가지 종류가 있음

<%! 자바 코드 ;%> 각행은 세미콜론으로 끝나야 함
<%= 자바코드 ;%> 

선언문과 스크립틀릿 비교

선언문 : 변수뿐 아니라 매서드 선언도 할 수 있음
스크립틀릿 : 변수 선언만 할 수 있음
